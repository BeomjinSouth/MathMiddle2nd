/**
 * Vite Environment Type Definitions
 * 
 * This file provides TypeScript type definitions for Vite's client-side APIs.
 * It includes types for import.meta.env, import.meta.hot, and other Vite-specific features.
 * 
 * The triple-slash directive below imports Vite's built-in type definitions
 * which enable TypeScript support for:
 * - Environment variables (import.meta.env)
 * - Hot Module Replacement (import.meta.hot)
 * - Static asset imports (e.g., importing .png files)
 * - Dynamic imports with Vite-specific features
 */

/// <reference types="vite/client" />
